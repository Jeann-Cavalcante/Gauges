import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
  },
  button: {
    flex: 1,
    padding: 10,
    borderRadius: 5,
    backgroundColor: "#fff",
  },
  text: {
    textAlign: "center",
    color: "#004589",
    fontSize: 16,
    fontWeight: "bold",
  },
});